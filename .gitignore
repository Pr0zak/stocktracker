# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
/app/build/
/shared/build/
/wear/build/
/app/release/

# Signing / secrets — never commit
*.jks
*.keystore
keystore.properties
key.properties

# Stitch design mockups (local reference only)
/.stitch-mockups/

# Misc
*.log
local.env

# Local session task tracker (rehydration scratch, not for the public repo)
/docs/TODO.md

# Design-canvas build output. The .dc.html artboards and canvas.json are the sources and are
# tracked; this is the ~2.6 MB page the seeder assembles from them, regenerated on every change.
/design/canvas/stocktracker-repair-in-place.html

# Scratch git worktrees for parallel agent work — never part of a commit.
.claude/wt/
