<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base window theme. Compose draws the real UI; this only styles the
         window before/around Compose (background, no action bar). -->
    <style name="Theme.StockTracker" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
